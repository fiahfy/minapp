{
  "name": "minapp",
  "description": "Minimum app boilerplate",
  "version": "1.0.0",
  "devDependencies": {
    "netlify-cli": "^2.6.0",
    "parcel-bundler": "^1.11.0"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html",
    "deploy": "netlify deploy -d dist",
    "dev": "parcel src/index.html"
  }
}
