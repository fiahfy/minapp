# minapp

> Minimum app boilerplate.

``` bash
# install dependencies
yarn

# serve with hot reload
yarn dev [--open]

# building for production
yarn build

# deploy to netlify
yarn deploy [--prod]
```
